From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Ivo Quiring <falk.quiring@gmail.com>
Date: Wed, 20 Aug 2025 11:31:51 +0200
Subject: [PATCH] Add pulvinar instance


diff --git a/net/minecraft/server/Main.java b/net/minecraft/server/Main.java
index b06c2c4aa77edafb374f7cf0406cf4d29c6e7f9f..999f9f9dbeb4a73fbdd2f34a05a8f8b7a705a24c 100644
--- a/net/minecraft/server/Main.java
+++ b/net/minecraft/server/Main.java
@@ -1,6 +1,5 @@
 package net.minecraft.server;
 
-import com.mojang.authlib.yggdrasil.YggdrasilAuthenticationService;
 import com.mojang.datafixers.DataFixer;
 import com.mojang.logging.LogUtils;
 import com.mojang.serialization.Dynamic;
@@ -16,12 +15,10 @@ import java.nio.file.Paths;
 import java.util.Optional;
 import java.util.function.BooleanSupplier;
 import javax.annotation.Nullable;
-import joptsimple.OptionParser;
+
 import joptsimple.OptionSet;
-import joptsimple.OptionSpec;
-import joptsimple.util.PathConverter;
+import net.kissenpvp.api.base.Pulvinar;
 import net.minecraft.CrashReport;
-import net.minecraft.DefaultUncaughtExceptionHandler;
 import net.minecraft.SharedConstants;
 import net.minecraft.SuppressForbidden;
 import net.minecraft.Util;
@@ -175,6 +172,13 @@ public class Main {
 
             org.spigotmc.SpigotConfig.disabledAdvancements = spigotConfiguration.getStringList("advancements.disabled"); // Paper - fix SPIGOT-5885, must be set early in init
 
+            // Pulvinar start - hook in kissen
+            LOGGER.info("Pulvinar is now going to hook in pulvinar.");
+            java.io.File pulvinarSettings = (java.io.File) optionSet.valueOf("pulvinar-settings");
+            Pulvinar pulvinar = new net.kissenpvp.base.PulvinarCore(pulvinarSettings);
+            org.bukkit.Bukkit.setPulvinar(pulvinar);
+            // Pulvinar end - hook in kissen
+
             // Paper start - fix SPIGOT-5824
             File file;
             File userCacheFile = new File(Services.USERID_CACHE_FILE);
diff --git a/net/minecraft/server/Services.java b/net/minecraft/server/Services.java
index 015e3ff8d856a7722def4448d8c68792cc3a32a2..6ab9257e581fdd10872c8be2fd5317d9606b0a78 100644
--- a/net/minecraft/server/Services.java
+++ b/net/minecraft/server/Services.java
@@ -29,7 +29,7 @@ public record Services(
     public static Services create(YggdrasilAuthenticationService authenticationService, File profileRepository, File userCacheFile, joptsimple.OptionSet optionSet) throws Exception { // Paper - add optionset to load paper config files; add userCacheFile parameter
         MinecraftSessionService minecraftSessionService = authenticationService.createMinecraftSessionService();
         GameProfileRepository gameProfileRepository = authenticationService.createProfileRepository();
-        GameProfileCache gameProfileCache = new GameProfileCache(gameProfileRepository, userCacheFile); // Paper - use specified user cache file
+        GameProfileCache gameProfileCache = new net.kissenpvp.network.actor.PulvinarProfileCache(gameProfileRepository); // new GameProfileCache(gameProfileRepository, userCacheFile); // Paper - use specified user cache file // Pulvinar - delegate caching to database
         // Paper start - load paper config files from cli options
         final java.nio.file.Path legacyConfigPath = ((File) optionSet.valueOf("paper-settings")).toPath();
         final java.nio.file.Path configDirPath = ((File) optionSet.valueOf("paper-settings-directory")).toPath();
diff --git a/net/minecraft/server/commands/BanPlayerCommands.java b/net/minecraft/server/commands/BanPlayerCommands.java
index ac3ba9d0ea344fa189912d359b718fbe05e7aa49..cc5b362d661e887911dbcf69d082b9b6cef9c3bd 100644
--- a/net/minecraft/server/commands/BanPlayerCommands.java
+++ b/net/minecraft/server/commands/BanPlayerCommands.java
@@ -47,9 +47,11 @@ public class BanPlayerCommands {
 
         for (GameProfile gameProfile : gameProfiles) {
             if (!bans.isBanned(gameProfile)) {
-                UserBanListEntry userBanListEntry = new UserBanListEntry(
+                // Pulvinar start - use database ban system instead of vanilla
+                UserBanListEntry userBanListEntry = createEntry(source, gameProfile, reason); /*new UserBanListEntry(
                     gameProfile, null, source.getTextName(), null, reason == null ? null : reason.getString()
-                );
+                );*/
+                // Pulvinar end - use database ban system instead of vanilla
                 bans.add(userBanListEntry);
                 i++;
                 source.sendSuccess(
@@ -68,4 +70,34 @@ public class BanPlayerCommands {
             return i;
         }
     }
+
+    // Pulvinar start - use database ban system instead of vanilla
+    private static net.kissenpvp.punishment.PunishmentEntry createEntry(CommandSourceStack source, GameProfile gameProfile, @Nullable Component reason)
+    {
+        net.kissenpvp.api.temporal.timespan.TimeSpan temporal = new net.kissenpvp.temporal.timespan.PermanentTimeSpan();
+        net.kissenpvp.temporal.InternalWritableTemporalObject permanent = net.kissenpvp.temporal.InternalWritableTemporalObject.toTemporal(temporal);
+        net.kyori.adventure.text.Component component = null;
+        if (java.util.Objects.nonNull(reason))
+        {
+            component = net.kyori.adventure.text.Component.text(reason.getString());
+        }
+
+        java.util.UUID sourceUuid = null;
+        if(source.isPlayer())
+        {
+            sourceUuid = java.util.Objects.requireNonNull(source.getPlayer()).getUUID();
+        }
+
+        // Look - I know this is stupid and I promise I will come back later for this...
+        java.util.UUID linkId = org.bukkit.Bukkit.getOfflinePlayer(gameProfile.getId()).linkId();
+
+        return net.kissenpvp.punishment.PunishmentEntry.from(new net.kissenpvp.punishment.InternalPunishmentSubscription(
+                -1,
+                linkId,
+                sourceUuid,
+                permanent,
+                component
+        ));
+    }
+    // Pulvinar end - use database ban system instead of vanilla
 }
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index 577bb59afb4e059523e68af6c3515d64f0b9e559..907334d40e7748aad6d793d634e32a9d3115506e 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -2296,14 +2296,24 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
 
     public void sendSystemMessage(Component message, boolean overlay) {
         if (this.acceptsSystemMessages(overlay)) {
+
+            // Pulvinar start - implement system message event
+            net.kyori.adventure.text.Component adventure = io.papermc.paper.adventure.PaperAdventure.asAdventure(message);
+            net.kissenpvp.api.event.SystemMessageEvent messageEvent = new net.kissenpvp.api.event.SystemMessageEvent(adventure);
+            org.bukkit.Bukkit.getPluginManager().callEvent(messageEvent);
+
+            if(messageEvent.isCancelled()) { return; }
+            Component outputMessage = io.papermc.paper.adventure.PaperAdventure.asVanilla(messageEvent.getMessage());
+            // Pulvinar end - implement system message event
+
             this.connection
                 .send(
-                    new ClientboundSystemChatPacket(message, overlay),
+                    new ClientboundSystemChatPacket(outputMessage /*message*/, overlay),  // Pulvinar - implement system message event
                     PacketSendListener.exceptionallySend(
                         () -> {
                             if (this.acceptsSystemMessages(false)) {
                                 int i = 256;
-                                String string = message.getString(256);
+                                String string = outputMessage.getString(256); //message.getString(256); // Pulvinar - implement system message event
                                 Component component = Component.literal(string).withStyle(ChatFormatting.YELLOW);
                                 return new ClientboundSystemChatPacket(
                                     Component.translatable("multiplayer.message_not_delivered", component).withStyle(ChatFormatting.RED), false
diff --git a/net/minecraft/server/players/BanListEntry.java b/net/minecraft/server/players/BanListEntry.java
index e111adec2116f922fe67ee434635e50c60dad15c..7cf45d0cf8767f3ba798b8641ed6b92b456fb55f 100644
--- a/net/minecraft/server/players/BanListEntry.java
+++ b/net/minecraft/server/players/BanListEntry.java
@@ -70,7 +70,7 @@ public abstract class BanListEntry<T> extends StoredUserEntry<T> {
 
     @Override
     boolean hasExpired() {
-        return this.expires != null && this.expires.before(new Date());
+        return this.getExpires() != null && this.getExpires().before(new Date()); // Pulvinar - use getter instead of field to allow override
     }
 
     @Override
diff --git a/net/minecraft/server/players/GameProfileCache.java b/net/minecraft/server/players/GameProfileCache.java
index 066f84df5c31242ab542932f1e243369d0e766e2..a34b21bccb123525806f1ff6f7230d93da2a5b2b 100644
--- a/net/minecraft/server/players/GameProfileCache.java
+++ b/net/minecraft/server/players/GameProfileCache.java
@@ -317,12 +317,12 @@ public class GameProfileCache {
         }
     }
 
-    static class GameProfileInfo {
+    protected static class GameProfileInfo { // Pulvinar - allow protected access
         private final GameProfile profile;
         final Date expirationDate;
         private volatile long lastAccess;
 
-        GameProfileInfo(GameProfile profile, Date expirationDate) {
+        public GameProfileInfo(GameProfile profile, Date expirationDate) {  // Pulvinar - allow public access
             this.profile = profile;
             this.expirationDate = expirationDate;
         }
@@ -343,4 +343,19 @@ public class GameProfileCache {
             return this.lastAccess;
         }
     }
+
+    // Pulvinar start - allow protected access to the profile repository and executor
+    protected Map<String, CompletableFuture<Optional<GameProfile>>> getRequests()
+    {
+        return requests;
+    }
+
+    protected @Nullable Executor getExecutor() {
+        return this.executor;
+    }
+
+    protected GameProfileRepository getRepository() {
+        return this.profileRepository;
+    }
+    // Pulvinar end - allow protected access to the profile repository and executor
 }
diff --git a/net/minecraft/server/players/PlayerList.java b/net/minecraft/server/players/PlayerList.java
index db56578bff06574337463c2421537f43d1a19189..2e37d716e5d84c8b2e47438f4df694250067b0c2 100644
--- a/net/minecraft/server/players/PlayerList.java
+++ b/net/minecraft/server/players/PlayerList.java
@@ -113,9 +113,9 @@ public abstract class PlayerList {
     private final MinecraftServer server;
     public final List<ServerPlayer> players = new java.util.concurrent.CopyOnWriteArrayList(); // CraftBukkit - ArrayList -> CopyOnWriteArrayList: Iterator safety
     private final Map<UUID, ServerPlayer> playersByUUID = Maps.newHashMap();
-    private final UserBanList bans = new UserBanList(USERBANLIST_FILE);
+    private final UserBanList bans = new net.kissenpvp.punishment.PunishmentList(((net.kissenpvp.api.database.RepositoryHolder) org.bukkit.Bukkit.getPulvinar()).punishmentSubscriptionRepository()); // new UserBanList(USERBANLIST_FILE); // Pulvinar - migrate ban operations to database
     private final IpBanList ipBans = new IpBanList(IPBANLIST_FILE);
-    private final ServerOpList ops = new ServerOpList(OPLIST_FILE);
+    private final ServerOpList ops = new net.kissenpvp.network.actor.operator.PulvinarOperatorList(((net.kissenpvp.api.database.RepositoryHolder) org.bukkit.Bukkit.getPulvinar()).operatorRepository()); // new ServerOpList(OPLIST_FILE); // Pulvinar - migrate operator data to database
     private final UserWhiteList whitelist = new UserWhiteList(WHITELIST_FILE);
     // CraftBukkit start
     // private final Map<UUID, ServerStatsCounter> stats = Maps.newHashMap();
diff --git a/net/minecraft/server/players/ServerOpListEntry.java b/net/minecraft/server/players/ServerOpListEntry.java
index aa2663be118b628257e16e3da309e8a3b4f9ad71..f01f19d97161ad109aaa06c9d52e7fe23f6b83c3 100644
--- a/net/minecraft/server/players/ServerOpListEntry.java
+++ b/net/minecraft/server/players/ServerOpListEntry.java
@@ -2,10 +2,12 @@ package net.minecraft.server.players;
 
 import com.google.gson.JsonObject;
 import com.mojang.authlib.GameProfile;
+import org.jetbrains.annotations.NotNull;
+
 import java.util.UUID;
 import javax.annotation.Nullable;
 
-public class ServerOpListEntry extends StoredUserEntry<GameProfile> {
+public class ServerOpListEntry extends StoredUserEntry<GameProfile> implements net.kissenpvp.api.network.actor.OperatorInfo { // Pulvinar start - migrate operator data to database
     private final int level;
     private final boolean bypassesPlayerLimit;
 
@@ -56,4 +58,36 @@ public class ServerOpListEntry extends StoredUserEntry<GameProfile> {
             return null;
         }
     }
+
+    // Pulvinar start - migrate operator data to database
+    @Override public UUID id()
+    {
+        if(java.util.Objects.isNull(getUser()))
+        {
+            throw new java.lang.IllegalStateException("User is null");
+        }
+
+        return getUser().getId();
+    }
+
+    @Override public String name()
+    {
+        if(java.util.Objects.isNull(getUser()))
+        {
+            throw new java.lang.IllegalStateException("User is null");
+        }
+
+        return getUser().getName();
+    }
+
+    @Override public int signature()
+    {
+        return java.util.Objects.hash(id(), level, bypassesPlayerLimit);
+    }
+
+    @Override public boolean unsaved()
+    {
+        return false;
+    }
+    // Pulvinar end - migrate operator data to database
 }
diff --git a/net/minecraft/server/players/StoredUserList.java b/net/minecraft/server/players/StoredUserList.java
index d445e8f126f077d8419c52fa5436ea963a1a42a4..a0c9255e1cce64c733cc70d4a523912f7cab05d0 100644
--- a/net/minecraft/server/players/StoredUserList.java
+++ b/net/minecraft/server/players/StoredUserList.java
@@ -132,4 +132,11 @@ public abstract class StoredUserList<K, V extends StoredUserEntry<K>> {
             // Spigot end
         }
     }
+
+    // Pulvinar start - allow protected access to map
+    protected Map<String, V> getMap()
+    {
+        return map;
+    }
+    // Pulvinar end - allow protected access to map
 }
diff --git a/net/minecraft/world/level/storage/PlayerDataStorage.java b/net/minecraft/world/level/storage/PlayerDataStorage.java
index fe44d8d17d2622b3d6021c11579af85ef96737bb..6207758d31408c4f95507c72787b4e9006b1f87c 100644
--- a/net/minecraft/world/level/storage/PlayerDataStorage.java
+++ b/net/minecraft/world/level/storage/PlayerDataStorage.java
@@ -36,6 +36,26 @@ public class PlayerDataStorage {
         try (ProblemReporter.ScopedCollector scopedCollector = new ProblemReporter.ScopedCollector(player.problemPath(), LOGGER)) {
             TagValueOutput tagValueOutput = TagValueOutput.createWithContext(scopedCollector, player.registryAccess());
             player.saveWithoutId(tagValueOutput);
+
+            // Pulvinar start - store userdata in a database instead of a world
+            net.kissenpvp.api.database.RepositoryHolder pulvinar = (net.kissenpvp.api.database.RepositoryHolder) org.bukkit.Bukkit.getPulvinar();
+            net.kissenpvp.network.actor.InternalPlayerRepository playerRepository = (net.kissenpvp.network.actor.InternalPlayerRepository) pulvinar.playerRepository();
+
+            CompoundTag tag = tagValueOutput.buildResult();
+
+            try (java.io.ByteArrayOutputStream byteArrayOutputStream = new java.io.ByteArrayOutputStream()) {
+                net.minecraft.nbt.NbtIo.writeCompressed(tag, byteArrayOutputStream);
+                byte[] data = byteArrayOutputStream.toByteArray();
+                playerRepository.saveProperties(player.getUUID(), data);
+            }
+            catch (java.io.IOException ioException)
+            {
+                ioException.printStackTrace();
+            }
+
+            if(true) { return; } // we're skipping the file part
+            // Pulvinar end - store userdata in a database instead of a world
+
             Path path = this.playerDir.toPath();
             Path path1 = Files.createTempFile(path, player.getStringUUID() + "-", ".dat");
             CompoundTag compoundTag = tagValueOutput.buildResult();
@@ -49,6 +69,11 @@ public class PlayerDataStorage {
     }
 
     private void backup(String name, String stringUuid, String suffix) { // CraftBukkit
+
+        // Pulvinar start - store userdata in a database instead of a world
+        if (true) { return; } // we don't do that here
+        // Pulvinar end - store userdata in a database instead of a world
+
         Path path = this.playerDir.toPath();
         Path path1 = path.resolve(stringUuid + suffix); // CraftBukkit
         Path path2 = path.resolve(stringUuid + "_corrupted_" + LocalDateTime.now().format(FORMATTER) + suffix); // CraftBukkit
@@ -62,6 +87,23 @@ public class PlayerDataStorage {
     }
 
     private Optional<CompoundTag> load(String name, String stringUuid, String suffix) { // CraftBukkit
+
+        // Pulvinar start - store userdata in a database instead of a world
+        net.kissenpvp.api.database.RepositoryHolder pulvinar = (net.kissenpvp.api.database.RepositoryHolder) org.bukkit.Bukkit.getPulvinar();
+        net.kissenpvp.network.actor.InternalPlayerRepository playerRepository = (net.kissenpvp.network.actor.InternalPlayerRepository) pulvinar.playerRepository();
+
+        Optional<java.sql.Blob> data = playerRepository.findProperties(java.util.UUID.fromString(stringUuid)).join();
+        return data.map(blob -> {
+            try {
+                return NbtIo.readCompressed(blob.getBinaryStream(), NbtAccounter.unlimitedHeap());
+            }
+            catch (java.sql.SQLException | java.io.IOException exception)
+            {
+                LOGGER.warn("Failed to load player data for {}.", name, exception);
+                return null;
+            }
+        });
+        /*
         File file = new File(this.playerDir, stringUuid + suffix); // CraftBukkit
         // Spigot start
         boolean usingWrongFile = false;
@@ -88,6 +130,8 @@ public class PlayerDataStorage {
         }
 
         return Optional.empty();
+        */
+        // Pulvinar end - store userdata in a database instead of a world
     }
 
     public Optional<ValueInput> load(Player player, ProblemReporter problemReporter) {
